
# JavaScript Day 4 agenda

## inserAdjacentELement
 - syntax
 - positions: beforebegin, afterbegin, beforeend, afterend


## events
 - types of events **inline html, add event listeners**
    - mouse events (click, dblclick, mouseover, mouseout, mousemove, mousedown, mouseup)
    - keyboard events (keydown, keyup, keypress)
    - form events (submit, change, focus, blur)
    - window events (load, resize, scroll, unload)
 - event object
 - event propagation (bubbling and capturing)
 - event delegation
 - preventDefault and stopPropagation

 ## JSON
    - what is JSON
    - json.stringify()
    - json.parse()

## Cookies 
## AJAX
 - working with AJAX with JSON 
 - XMLHttpRequest
 - fetch API
 - handling responses
 - error handling
 (#TODO: local& session Storage, Regular Expressions (REGEX) )


 omar.sayHI()